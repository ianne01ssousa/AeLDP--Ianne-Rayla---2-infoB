programa {
  funcao inicio() {
    //Declaração de variavel
    inteiro num, centena, dezena, unidade, cont 

    //Entrada de Dados 
    escreva("Informe um número com três digitos: ")
    leia(num)

    //processamento
    centena = num / 100
    cont = num % 100
    dezena = cont / 10
    cont = cont % 10
    unidade = cont
    escreva("O número ao contrário é ", unidade, dezena, centena)
  }
}
